package at.iaik.websharkgwt.shared.utils;

import java.util.Enumeration;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class HttpResponseDebugger {
	
	public static void printRequest(HttpServletResponse resp) {
		// TODO Auto-generated method stub
		
		//System.out.println(req.getAuthType());
		//System.out.println(req.getCharacterEncoding());
		//System.out.println(req.getContentLength());
		//System.out.println(req.getHeader(null));
		//Enumeration headerNames = resp.getHeaderNames();
		/*while(headerNames.hasMoreElements()) {
		      String headerName = (String)headerNames.nextElement();
		      System.out.println(headerName + "   : " + req.getHeader(headerName));
		    }
		*/
		System.out.println(resp.toString());
		
	}

}
